<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.boardTest.mapper.BoardMapper">

    <select id="selectBoardList" resultType="Board">
            SELECT
                *
            FROM
                BOARD
    </select>

    <select id="selectBoardId">
        SELECT *
        FROM BOARD B
        WHERE B.NUM = #{num}
    </select>

    <insert id="insertBoard" parameterType="Board">
        INSERT
        INTO
            BOARD
            (B.NUM, B.TITLE, B.NAME, B.CONTENT)
        VALUES (num.nextval, #{title}, #{name}, #{content})
    </insert>

    <select id="selectReadData" parameterType="java.lang.Integer" resultType="Board" >
        SELECT
            B.NUM,
            B.TITLE,
            B.NAME,
            B.CONTENT,
            B.HIT
        FROM BOARD B
        WHERE B.NUM = #{num}
    </select>

    <update id="updateViewCount" parameterType="java.lang.Integer">
        UPDATE BOARD B SET B.HIT = B.HIT + 1 WHERE B.NUM=#{num}
    </update>

    <update id="updateBoard" parameterType="Board">
        UPDATE BOARD B
            SET
                B.BOARD_TITLE = #{title},
                B.BOARD_NAME = #{name},
                B.BOARD_CONTENT = #{content}
            WHERE B.NUM = #{num}
    </update>

    <delete id="deleteBoard" parameterType="java.lang.Integer">
        DELETE BOARD B where B.NUM=#{num}
    </delete>

</mapper>

